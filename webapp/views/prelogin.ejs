<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>HCMUT Tutor System</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <style>
      /* Background wrapper */
      body::before {
        content: "";
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: url("/background.jpeg") center/cover no-repeat;
        filter: grayscale(70%) blur(6px);
        z-index: -1; /* behind everything */
        opacity: 0.7;
      }

      /* Optional: Dark overlay for readability */
      body::after {
        content: "";
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.25);
        z-index: -1;
      }

      h1 {
        color: white;
        text-shadow: 0 2px 6px rgba(0, 0, 0, 0.6);
      }

      footer small {
        color: white;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.6);
      }
    </style>
  </head>

  <body class="d-flex flex-column min-vh-100 text-light">
    <main class="flex-fill text-center d-flex flex-column align-items-center justify-content-center">
      
        <h1>Welcome to HCMUT Tutor System</h1>
        
          <% if (typeof error !== "undefined" && error) { %>
            <div class="alert alert-danger alert-dismissible fade show">
              <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
              <strong>Error!</strong> Internal server error, please try again later
            </div>
          <% } %>
        <a class="btn btn-primary mt-3" href="<%= oauth2link %>">Login using SSO</a>
    </main>

    <footer class="text-center py-3 fixed-bottom">
      <small>
        Semester 251 of Software Engineering (CO3001) - Developed by Group Happy Birthday
      </small>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
